shared enum MapMood {
    Day = 0,
    Night = 1,
    Sunset = 2,
    Sunrise = 3,
}

shared enum MapBase {
    NoStadium = 32,
    StadiumOld = 64,
    Stadium155 = 128,
}

shared enum MapCar {
    CarSport,
    CarSnow,
    CarRally,
    CarDesert,
}
